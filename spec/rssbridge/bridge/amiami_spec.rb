# frozen_string_literal: true

RSpec.describe Rssbridge::Bridge::Amiami do
  it "has a version number" do
    expect(Rssbridge::Bridge::Amiami::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
